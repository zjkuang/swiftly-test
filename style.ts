import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  parentView: {
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    height: '100%',
  },
  childView: {
    justifyContent: 'center',
    alignItems: 'center',
    width: '100%',
    height: 60,
    marginHorizontal: 8,
  },
});
